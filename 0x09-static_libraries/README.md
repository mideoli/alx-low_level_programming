# 0x09. C - Static libraries

## Tasks

0. Create the static library libmy.a containing all the functions listed on the main.h.

1. Create a script called <code>create_static_lib.sh</code> that creates a static library called <code>liball.a</code> from all the <code>.c</code> files that are in the current directory.
