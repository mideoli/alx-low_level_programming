#include "main.h"
/**
 * main - prints the alphabet in lowercase
 *
 * Return: Always 0.
 */

int main(void)
{
	int alphabet = 'a';
	
	while (alphabet <= 'z')
	{
		_putchar(alphabet);
	}
	_putchar('\n');
	return (0);
}
