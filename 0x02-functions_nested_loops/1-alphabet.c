#include "main.h"
/**
 * main - prints the alphabet in lowercase
 *
 * Return: Always 0.
 */

int main(void)
{
	char alphabet = 'a';
	
	while (alphabet <= 'z')
	{
		puts(alphabet);
	}
	puts('\n');
	return (0);
}
