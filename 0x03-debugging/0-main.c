#include "main.h"

/**
 * main - Entry point.
 * Description: This program test the function positive_or_negative()
 *		gives the correct output when given case of 0.
 *
 * Return: 0 (Success).
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);
	
	return (0);
}
